class AppConfig {
  static const String appName = 'Minang Food Classifier';
  static const bool debugMode = true;
  
  static const Duration apiTimeout = Duration(seconds: 30);
  static const String envFileName = '.env';
}